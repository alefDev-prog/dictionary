import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    information: {}
}

const infoSlice = createSlice({
    name:'infoSlice',
    initialState,
    reducers: {
        
    }
})